import React from 'react';

const ReviewModelReducer = (id, author_name, title, timestamp, rating, comment) => {
    string: id;
    string: author_name;
    string: title;
    DateTime: timestamp;
    int: rating;
    string: comment;
}
